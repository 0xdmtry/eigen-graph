"use client";
import React from "react";
import type {TableItem} from "@/types/operators";

type Props = {
    tokens: Record<string, TableItem[]>;
};

export default function TokenPanelDnD(_: Props) {
    return <div />;
}
