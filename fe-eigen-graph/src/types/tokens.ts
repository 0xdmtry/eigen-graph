export interface TokenCardType {
    symbol: string;
    name: string;
    icon: string;
    tvl: string;
    operators: number;
}

export interface TokenCardDnDType {
    id: string;
    symbol: string;
    name: string;
    icon: string;
    tvl: string;
    operators: number;
}
